import java.util.*;

public class stdinLoop {
    /* constant declarations */
    
    /* Inner class declaration */
    private static class Name {
        
        /* Member declarations */
        
        /* Methods */
        
    }
    
    /* global declarations (arrays, array sizes etc) */
    
    private static void main(String args[]) {
        
        Scanner in = new Scanner(System.in);
        
        //get start case input
        
        while (in.hasNext() or some terminating condition) {
            
            //continue case input
            
            //process data
            
            //get next case input if a terminating conditon exists (like a 0 ends input)
            if there's a terminating condition
                get start of next case
        }
    }
}